mutation addBook($input: AddBookInput!) {
  addBook(input: $input) {
    book {
      id
      issueDate
      dueDate
      receivedDate
      noOfCopiesAvailable
      status
      student {
        id
      }
      library {
        id
      }
    }
  }
}
